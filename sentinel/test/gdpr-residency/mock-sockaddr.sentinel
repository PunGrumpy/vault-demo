# Mock sockaddr import for testing
# Checks if an IP address is contained within a CIDR block
is_contained = func(ip, cidr) {
    # For testing: check if IP matches the CIDR
    # CIDR "129.41.56.7/32" means exact match for 129.41.56.7
    if cidr == "129.41.56.7/32" {
        return ip == "129.41.56.7"
    }
    # For other CIDRs, you could implement more complex logic
    # But for this test, we only need to check exact match
    return false
}

