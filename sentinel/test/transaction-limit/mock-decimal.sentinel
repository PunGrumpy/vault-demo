# Mock decimal import for testing
# Creates a decimal value from a string
# Sentinel's decimal.new() returns a decimal type that supports comparison
new = func(value) {
    # Convert string to number for comparison
    # Sentinel will handle the decimal comparison
    num_value = float(value)
    return num_value
}

